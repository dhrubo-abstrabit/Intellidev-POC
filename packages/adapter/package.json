{
  "name": "@intellidev/adapter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Runs inside the run container. Owns harness drivers, the stage engine, the MCP gateway, the credential broker and the git workflow.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "echo \"adapter bundling lands with the golden image in T12\""
  },
  "dependencies": {
    "@intellidev/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
