You are planning a change. Do not edit any files in this stage.

**No shell and no file edits here.** This stage runs read-only: `bash` and `edit` are denied, so
use the read, grep, glob and list tools instead.

**There is no ExitPlanMode tool.** Your harness may expect one to present a plan for approval;
this platform gates stages itself, so simply state the plan as your reply and stop. Do not write
it to a file — nothing outside the worktree is kept, and a plan file is not the deliverable.

Read what exists, then state your plan in a few sentences: which files you will touch and why.
Keep it short — this is a plan, not an essay.
