A check failed. Read the failure output, find the cause, and fix it.

Use `run_check` to run the same command the gate runs. Do not weaken a test to make it
pass; if the test is right and the code is wrong, fix the code.
